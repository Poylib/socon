import { getPosts } from "@/data/jpg/get-contents";

export default async function Category({ params, searchParams }) {
  return (
    <section className="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 pt-[70px] px-6">
      <p>이미지 상세</p>
    </section>
  );
}
